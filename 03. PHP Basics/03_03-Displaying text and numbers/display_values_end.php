<?php // no need to use closing php tag since we only use php
$firstName = 'David'; // datatype of firstName is string
// but we don't need to specify the data type 
$prénom = 'David';
//$first-Name = 'David';
//$1stName = 'David';
$number = 4200.99; // comma is not used for decimal point

echo $number, " ", $firstName;
echo ($number); // paranthesis is optional
// echo can display series of values seperated by commas
echo $number, ' '; // single or double quote doesn't matter
print $prénom; // unlike js semicolon is not optional